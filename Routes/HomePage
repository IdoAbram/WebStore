const express = require("express")
const router = express.Router();
const products = require("../Model/Schemas/Product");

router.get('/',{products})
//router.post("/", loginController.login);

module.exports = router;